**VibeSeek** — Windows (x64) 安装包。

> ⚠️ **未签名提示**:应用暂未做代码签名,首次运行时 Windows SmartScreen 可能提示"已保护你的电脑"。点击「更多信息」→「仍要运行」即可,这是未签名应用的正常表现。

### 校验完整性

下载后比对 `SHA256SUMS.txt` 中的哈希:

```powershell
Get-FileHash .\VibeSeek-Setup-*.exe -Algorithm SHA256
```
